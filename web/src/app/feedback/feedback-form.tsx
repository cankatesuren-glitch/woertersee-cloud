"use client";import{FormEvent,useState}from"react";
export default function FeedbackForm(){const[state,setState]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/feedback",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type:f.get("type"),subject:f.get("subject"),message:f.get("message")})});if(r.ok){e.currentTarget.reset();setState("Thank you — your feedback is in the review queue.")}else setState((await r.json()).detail??"Feedback could not be sent.")}return <main className="words-shell"><header><a className="brand" href="/">Wörter<span>See</span></a><a href="/play">Back to learning</a></header><section className="words-intro"><p className="eyebrow">FEEDBACK</p><h1>Help WörterSee<br/>learn, too.</h1><p>Report a technical problem or suggest what would make your practice better.</p></section><section className="words-grid"><div><h2>What should we know?</h2></div><form className="word-form" onSubmit={submit}><label>Type<select name="type"><option value="GENERAL">General feedback</option><option value="TECHNICAL">Technical issue</option><option value="FEATURE">Feature suggestion</option></select></label><label>Subject<input name="subject" required maxLength={160}/></label><label>Message<textarea name="message" required maxLength={4000} rows={8}/></label><button className="primary">Send feedback</button><p aria-live="polite">{state}</p></form></section></main>}
